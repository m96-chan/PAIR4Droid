# Release build shrinking is disabled for now (see app/build.gradle.kts, isMinifyEnabled = false).
# Kept as a placeholder so re-enabling it later has somewhere to add UniFFI/JNA keep rules
# (JNA uses reflection into com.sun.jna.* and the generated uniffi.pair_ffi.* bindings).

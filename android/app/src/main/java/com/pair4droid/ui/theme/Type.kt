package com.pair4droid.ui.theme

import androidx.compose.material3.Typography

// The Material3 default type scale is fine for this app; declared explicitly only so
// Theme.kt has a single named typography value to pass in.
val Pair4Typography = Typography()
